[pytest]
# Run from `backend/` (same convention as alembic/uvicorn — see README) so
# `app`, `scripts` and `tests` all resolve as top-level packages without any
# extra path configuration.
testpaths = tests
python_files = test_*.py
addopts = -ra
