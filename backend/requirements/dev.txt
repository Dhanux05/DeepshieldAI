# Dev/test tooling. Deliberately depends on base.txt so API-only development
# does not install the large ML stack. Use ml.txt when testing detector,
# explainability, or RAG behavior.
-r base.txt

pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
httpx==0.28.1
factory-boy==3.3.3
ruff==0.16.1
black==26.5.1
mypy==2.3.0
