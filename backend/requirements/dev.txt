# Dev/test tooling for the current phase (Phase 4). Deliberately depends on
# base.txt, NOT ml.txt — Phase 4 has no ML code to test yet, and pulling in
# torch here would make plain backend development pay a multi-GB install
# for no benefit. Switch the line below to `-r ml.txt` once Phase 5 lands.
-r base.txt

pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
httpx==0.28.1
factory-boy==3.3.3
ruff==0.16.1
black==26.5.1
mypy==2.3.0
